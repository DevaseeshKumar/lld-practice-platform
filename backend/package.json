{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "jest --runInBand --detectOpenHandles",
    "seed": "node -e \"require('dotenv').config(); require('./config/db').connectDB().then((conn) => { if (!conn) { console.error('[Seed] DB connection failed'); process.exit(1); } return require('./config/seedData').seedDatabase(); }).then(() => process.exit(0)).catch(err => { console.error(err); process.exit(1); })\""
  },
  "keywords": [
    "lld",
    "practice",
    "system-design",
    "oop"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "mongoose": "^9.9.5"
  },
  "devDependencies": {
    "jest": "^30.5.1",
    "nodemon": "^3.1.14",
    "supertest": "^7.0.0"
  }
}
